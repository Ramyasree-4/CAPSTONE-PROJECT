import { GenericPage } from "@/components/page-kit";

export default function Page() {
  return <GenericPage title="Security Dashboard" subtitle="Prompt injection attempts, rate limits, failed logins, and security events." variant="admin" />;
}


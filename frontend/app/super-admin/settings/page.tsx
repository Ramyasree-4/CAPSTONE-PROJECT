import { GenericPage } from "@/components/page-kit";

export default function Page() {
  return <GenericPage title="System Configuration" subtitle="Global provider keys, retention, rate limits, and platform settings." variant="settings" />;
}


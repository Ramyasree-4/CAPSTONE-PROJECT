import { GenericPage } from "@/components/page-kit";

export default function Page() {
  return <GenericPage title="API Documentation" subtitle="Endpoint catalog for auth, documents, chat, analytics, feedback, evaluation, and admin APIs." variant="table" />;
}


import { GenericPage } from "@/components/page-kit";

export default function Page() {
  return <GenericPage title="Settings" subtitle="Theme, API keys, preferences, and model defaults." variant="settings" />;
}


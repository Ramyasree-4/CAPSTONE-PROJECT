import { GenericPage } from "@/components/page-kit";

export default function Page() {
  return <GenericPage title="Knowledge Graph Viewer" subtitle="Explore relationships between documents, chunks, entities, and citations." variant="graph" />;
}

